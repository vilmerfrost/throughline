# Copy to .env.local (gitignored) and fill in.
# Used ONLY by the analyzer server for the "Explain this node" feature.
# This key must NEVER be exposed to the frontend.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-3.5-flash
