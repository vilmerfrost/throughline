{
  "name": "throughline",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n analyzer,web -c blue,green \"pnpm --filter @throughline/analyzer dev\" \"pnpm --filter @throughline/web dev\"",
    "build": "pnpm typecheck && pnpm --filter @throughline/web build",
    "typecheck": "pnpm --filter @throughline/core exec tsc -p tsconfig.json --noEmit && pnpm --filter @throughline/analyzer exec tsc -p tsconfig.json --noEmit && pnpm --filter @throughline/web exec tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
